#include <bits/stdc++.h>
#include <windows.h>
using namespace std;
const int INF = 1e9;

// Путь с минимальной стоимостью во взвешенном графе: В графе с весами на ребрах найдите путь между двумя вершинами с минимальной стоимостью.

void dijkstra(vector<vector<int>> &graph, int startPoint, int endPoint, vector<int> &dist, vector<int> &path)
{
    int size_graph = graph.size();
    vector<bool> visited(size_graph, false);
}

int main() {
    cin.tie();
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);




    return 0;
}
