#include <bits/stdc++.h>
#include <windows.h>
using namespace std;

#define ar array
#define ll long long
#define FOR(i, a, b) for(int i = a; i < b; i++)

// Мы можем получить адрес переменной, используя операцию получения адреса &:

int main() {
    cin.tie();
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);

    // Определим три переменных и присвоим им некоторые значения
    int var1 = 11;
    int var2 = 22;
    int var3 = 33;

    // Напечатаем адреса этих переменных
    cout << &var1 << "\n" << &var2 << "\n" << &var3 << "\n";





   return 0;
}
