#include <bits/stdc++.h>
#include <windows.h>
#include <iterator>
#include <chrono>
#include <ctime>
using namespace std;

#define ar array
#define ll long long
#define FOR(i, a, b) for(int i = a; i < b; i++)

// Пример преобразования числа с плавающей запятой в целое число:

int main() {
    cin.tie();
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);

    double doubleValue = 10.5;
    int intValue = static_cast<int>(doubleValue);

    cout << "Преобразование числа с плавающей запятой в целое число: " << intValue << endl;



   return 0;
}

