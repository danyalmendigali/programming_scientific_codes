#include <bits/stdc++.h>
#include <windows.h>
#include <iterator>
#include <chrono>
#include <ctime>
using namespace std;

#define ar array
#define ll long long
#define FOR(i, a, b) for(int i = a; i < b; i++)

// Пример преобразования целого числа в число с плавающей запятой:

int main() {
    cin.tie();
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);

    int intValue = 10;
    double doubleValue = static_cast<double>(intValue);

    cout << "Преобразование целого числа в число с плавающей запятой: " << doubleValue << endl;




   return 0;
}

