#include <bits/stdc++.h>
#include <windows.h>
using namespace std;

#define ll long long
#define ar array
#define FOR(i, a, b) for(int i = a; i < b; i++)

// c_str(): Возвращает указатель на нуль-терминированную строку (массив символов) с тем же содержимым, что и у строки.

int main() {
    cin.tie(0);
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);

    string str = "Hello, World!";

    const char* cstr = str.c_str(); // cstr указывает на "Hello, World!\0"
    cout << cstr;










   return 0;
}








