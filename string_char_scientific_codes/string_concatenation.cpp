#include <bits/stdc++.h>
#include <windows.h>
using namespace std;

#define ar array
#define ll long long
#define FOR(i, a, b) for(int i = a; i < b; i++)

// Соединение строк:

int main() {
    cin.tie();
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);

    string str1 = "Hello,";
    string str2 = " world!";
    string result = str1 + str2; // Соединение строк

    cout << result << endl;




   return 0;
}

