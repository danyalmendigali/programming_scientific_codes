#include <bits/stdc++.h>
#include <windows.h>
using namespace std;

#define ll long long
#define ar array
#define FOR(i, a, b) for(int i = a, i < b; i++)

// length() или size(): Возвращает длину строки.

int main() {
    cin.tie();
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);

    string s;
    cout <<  "Введите строку: ";
    cin >> s;
    int length = s.size(); // или int length = s.length();
    cout << "Длинна строки: " << length;






   return 0;
}

