#include <bits/stdc++.h>
#include <windows.h>
using namespace std;

#define ll long long
#define ar array
#define FOR(i, a, b) for(int i = a; i < b; i++)

// Использование map с пользовательским компаратором для сортировки по ключу:

struct ReverseCompare {
    bool operator()(const string& str1, const string& str2) const {
        return str1 > str2;
    }
};

int main() {
    cin.tie(0);
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);

    map <string, int, ReverseCompare> scores;

    // Вставка элементов


  return 0;
}
















