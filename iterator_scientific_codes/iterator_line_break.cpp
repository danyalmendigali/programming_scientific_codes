#include <bits/stdc++.h>
#include <windows.h>
using namespace std;

#define ar array
#define ll long long
#define FOR(i, a, b) for(int i = a; i < b; i++)

// Использование итератора для разворота строки:


int main() {
    cin.tie();
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);

    string text = "Hello, world!";

    // Разворот строки с использованием итераторов
    reverse(text.begin(), text.end());

    cout << text << endl;



   return 0;
}














