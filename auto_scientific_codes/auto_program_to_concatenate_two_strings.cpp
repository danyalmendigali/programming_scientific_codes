#include <bits/stdc++.h>
#include <windows.h>
#include <iterator>
#include <fstream>
using namespace std;

#define ar array
#define ll long long
#define FOR(i, a, b) for(int i = a; i < b; i++)

// Программа для объединения двух строк:

int main() {
    cin.tie();
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);

    string str1 = "Hello";
    string str2 = "World";

    // Использование auto для определения типа переменной result
    auto result = str1 + " " + str2;

    cout << "Результат объединения: " << result << endl;


   return 0;
}




