#include <bits/stdc++.h>
#include <windows.h>
#include <iterator>
#include <sstream>
using namespace std;

#define ar array
#define ll long long
#define FOR(i, a, b) for(int i = a; i < b; i++)

// Проверка на пустоту set:

int main() {
    cin.tie();
    SetConsoleOutputCP(CP_UTF8);
    SetConsoleCP(CP_UTF8);

    set <int> set;

    if (set.empty()) {
        cout << "Сет пустой" << endl;
    } else {
        cout << "Сет не пустой" << endl;
    }


   return 0;
}





